<!DOCTYPE html>
<html lang="en">
    <!-- include partial head.ejs here -->
    <%- include('partials/head.ejs') %>
<body>
    <h1>Meme Index Page</h1>
    <a href = "/">Back to We Meme it Homepage</a>
    <a href = "/meme/new"> New Meme</a>
    <div>
        <% for(let i = 0; i < memes.length; i++){ %>
            <div>
                <a href="/meme/<%= memes[i]._id %>"><%=memes[i].title%></a></br>
                <img class = "homeImage" src="<%=memes[i].image%>" alt="<%=memes[i].title%>"/>
                <p><%=memes[i].description%></p>
                <div>
                    <form method="POST" action="/meme/<%= memes[i]._id %>?_method=Delete">
                        <input type="submit" value = "Delete Meme" />
                    </form>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>